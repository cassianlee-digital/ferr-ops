# DOMAIN 来自环境变量：
#   - 本地测试 DOMAIN=localhost → Caddy 用本地 CA 自签 https://localhost（浏览器可能提示，接受即可）
#   - 正式环境 DOMAIN=your-domain.com → Caddy 自动向 Let's Encrypt 申请并续期证书
{$DOMAIN:localhost} {
    encode gzip
    reverse_proxy app:3000
}
