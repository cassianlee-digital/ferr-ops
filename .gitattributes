# Normalize line endings to LF — avoids CRLF breaking shell scripts inside Docker
* text=auto eol=lf
*.png binary
*.jpg binary
*.ico binary
# 本地托管的 Web 字体：强制二进制，避免换行归一化损坏字体文件
*.woff binary
*.woff2 binary
*.ttf binary
*.eot binary
