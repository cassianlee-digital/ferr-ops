# deps
node_modules/
server/node_modules/

# 本地便携 Node 运行时(仅本机测试用，勿提交)
.tooling/

# secrets — NEVER commit
.env
*.env
!.env.example

# sqlite data + backups (persisted via mounted volume)
data/
backup/
*.sqlite
*.sqlite-*
*.db

# deploy bundles (may contain .env)
*.tar.gz

# logs / os
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# editor
.vscode/
.idea/

# Claude Code 本地机器配置(权限/本地路径，按机器走，勿提交)
.claude/settings.local.json

# 本机开发用:含绝对路径的便携 Node / 端口配置,不应入库
.claude/launch.json
