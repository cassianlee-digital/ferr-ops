# ferr-ops Project Status

## UI Upgrade Handoff - 2026-06-26

> 给下一台电脑 / 下一次 Codex 接续用。继续前请先读本段，再读 `AGENTS.md`。

### 当前 UI 方向

- 风格参考：阿里云控制台，不是营销页、炫酷大屏或花哨 SaaS。
- 核心气质：克制、清晰、可信、专业，像企业后台，不像临时拼出来的小工具。
- 视觉原则：
  - 白底为主，页面背景用很浅的灰。
  - 分割线用浅灰细线，不用大面积阴影和渐变。
  - 主色只保留少量蓝色，用于当前状态、主按钮、焦点输入框。
  - 标题少量加粗，正文保持中等字重，不要到处粗体。
  - 表格、输入框、按钮、标签统一高度、边框、圆角和间距。
  - 页面优先像控制台工作台：信息密度高但不乱，模块边界清楚。
- 禁止方向：
  - 不要做深色科技大屏。
  - 不要大面积彩色渐变、胶囊按钮、浮夸卡片、过重阴影。
  - 不要为了 UI 改接口、改数据保存、改 Google 同步逻辑。
  - 不要加入假数据或 demo 数据冒充真实结果。

### 本轮已经优化到哪里

主要文件：

- `public/styles.css`

已经追加了几组覆盖式样式，目的是在不大拆 `public/index.html` 的前提下，先把观感拉到统一控制台风格：

- 全局基线：
  - 字体、页面背景、主色、边框、按钮、输入框、表格、标签、弹窗、侧边导航做了统一。
  - 目标是把原来“七拼八凑”的视觉先收成一个系统。
- 左侧导航：
  - 已从偏暗、厚重的风格往更干净的后台导航靠拢。
  - 顶部 logo / favicon 已按用户给的红色 logo 做过替换相关改动，注意不要丢。
- 询盘评级里的全球询盘分布：
  - 旋转球已改为平面世界地图方向。
  - 用户最终更认可“蓝色世界地图 + 白色国家边界”的版本。
  - 地图需要继续检查高清显示、线条和标签清晰度，不要再用压扁或脏灰版本。
- 任务看板：
  - 已按阿里云风格收过一轮。
  - 三列任务区、SOP 区、任务卡片、状态标签、空状态、按钮层级已经更规整。
- 月度计划：
  - 已按“左右两个工作区 + 表格主体 + AI 建议区”的方式收过一轮。
  - 表格头、行高、空状态、新增按钮、AI 建议区已降噪。
- 测试登记：
  - 已跟月度计划一起收过一轮。
  - 重点是左右栏一致、表格干净、AI 建议不再像彩色贴片。

### 仍然需要继续优化的页面

下一步优先级建议：

1. 整改清单
   - 当前最乱：表格太挤，截止日期 / 结果 / 操作区域显得硬塞。
   - 建议方向：做成阿里云式数据表格，顶部筛选区、主操作按钮、表格列宽、状态标签统一。
   - 注意：只改样式，不改整改保存、归档、沉淀逻辑。

2. 设置页
   - 当前左侧设置菜单和右侧内容比例还不够像正式后台。
   - 建议方向：左侧为二级菜单，右侧为分组表单，输入项、状态项、说明文字统一。
   - API 接入页要继续诚实显示“未配置 / 待授权 / 未同步 / 失败原因”，不能假装已接入。

3. 数据看板 / KPI 考核
   - 需要统一卡片、表格、仪表盘和空状态。
   - 注意 `AGENTS.md` 已说明部分图表仍存在无数据示例问题，后续要避免假趋势。

4. 复盘 / SOP / 内容资产等剩余页面
   - 按同一套阿里云控制台规则继续收。
   - 每次只做一个页面或一个小区域。

### 右侧工具栏 / Hermes Agent 待办

用户想法：

- 参考阿里云右侧悬浮工具栏。
- 在后台右侧加一列轻量按钮，不要太多。
- 目前想放：
  - 设置按钮
  - Hermes AGENT 智能体按钮
- 点击 Hermes AGENT 后弹出一个面板 / 对话框。
- Hermes AGENT 计划由另一台服务器承载，本后台只做入口和嵌入 / 对接。
- 用户希望它不是普通 AI 分析，而是更像长期管家，能越来越懂业务。

建议实现顺序：

1. 先做纯前端入口壳：
   - 右侧固定窄工具栏。
   - 两个按钮：设置、Hermes AGENT。
   - 点击 Hermes AGENT 打开右下或右侧抽屉面板。
   - 面板先显示“智能体服务未配置 / 等待接入地址”，不要假装能用。

2. 再做配置项：
   - 在设置页增加 Hermes AGENT 服务地址配置。
   - 地址从后端读取，前端不要写死生产地址。

3. 最后做真实对接：
   - 后端代理请求 Hermes 服务器。
   - 加权限校验、失败提示、超时提示。
   - 不在前端暴露任何密钥。

注意：

- 右侧工具栏属于 UI / 交互增强，不应影响现有业务页面。
- 初版不要接真实 agent，先把入口和空状态做好。
- 不要用 iframe 直接硬嵌不可信页面，除非后续确认安全策略。

### 当前本地改动提醒

当前工作区有较多未提交改动，其中不少不是本轮 UI 交接文档产生的。继续前请先看 `git status`，不要随手回退用户或其他助手做过的改动。

已知主要相关文件：

- `public/styles.css`：本轮 UI 覆盖样式主要在这里。
- `public/index.html`：页面结构仍主要集中在这里，尽量不要大拆。
- `public/inquiry-globe.js`：全球询盘分布地图相关。
- `public/ferr-logo.png`：用户给的 logo / 图标资源。
- `public/world.geo.json`：世界地图底图资源。
- `public/closed-loop.js`：闭环页面逻辑，继续 UI 时尽量不要改业务逻辑。

### 继续操作时给 Codex 的推荐提示词

```text
请先读取 PROJECT_STATUS.md 里的 “UI Upgrade Handoff - 2026-06-26” 段，再读取 AGENTS.md。
继续按阿里云控制台风格优化 ferr-ops 后台 UI。
本次只做一个页面：先做“整改清单”。
只允许改样式或必要的轻量结构，不要改接口、数据保存、Google 同步、AI 逻辑。
修改前先说涉及文件，修改后给我：修改摘要 + 测试结果 + TODO。
```

## Current Mode

MVP 上线稳定版。

目标是尽快上线一个可用、稳定、可信的 SEO / SEM 运营后台，让团队先开始录入数据、查看数据、分析问题、生成整改建议。

不要陷入过度优化。当前阶段不追求完整自动化和精细化，只守住核心数据闭环不能出错。

## Working Rules

- 只做对上线有直接价值的事。
- 优先保证核心数据闭环：能新增、能显示、刷新后仍在、数据进入正确模块、失败有提示、不出现假成功。
- 重点避免低级错误：SEO 数据进 SEM、SEM 数据进 SEO、保存后丢失、刷新后消失、API 失败却显示成功。
- AI 建议必须基于真实数据库上下文，能说明问题、证据、建议动作、负责人或验证指标。
- 非阻断问题只记录 TODO，不现场修。
- 不做 GSC / GA4 / Google Ads OAuth，除非用户明确切到自动数据源阶段。
- 不做前端模块化、UI 美化、无关重构。
- 不为“更优雅”改已经能稳定工作的代码。
- Claude / Codex 交接只用 5 行：Task / Changed files / Checks / Commit / Blockers。

## Completed

- Phase 1e-a: `/api/inquiries` 支持 `start_date` / `end_date`，前端时间范围影响询盘。
- Phase 1e-b: `/api/seo-weeks`、`/api/sem-weeks` 支持日期区间；前端仅 SEO 折线图消费区间数据。
- Phase 1e-c: 前端增加时间范围作用边界提示，明确 KPI、SEM 看板、GA4、整改等暂不受时间范围影响。
- 核心数据 loader 失败提示：API 加载失败不再静默残留旧数据，给 toast + 清表。
- 关键词库修复：删除残留静态示例行，列对齐、单元格可真实录入入库。
- 闭环审计通过：核心数据录入 6 模块 + AI 建议采纳到整改/任务，均「新增→刷新仍在、失败有提示、无假成功」。
- 已生成 `DEPLOY_CHECKLIST.md`（启动/.env/首次部署/备份/手测/已知 TODO）。

## MVP Validation

- 已按 `DEPLOY_CHECKLIST.md` 完成上线前手测，结果：**登录、询盘、SEO 周报、SEM 周报、关键词、否词、广告创意、AI 建议采纳到整改/任务 —— 均无阻断问题**。
- 结论：**MVP 可进入部署 / 试用阶段。**

Latest pushed commit:

- `a55e623 docs: add deploy checklist`

## Known Local Difference

- `FERR运营后台_需求文档.md` 是用户主动删除的旧文件，不要恢复。
- 是否提交这个删除由用户单独决定，不要混入功能任务。
- `.claude/` 是本机协作文件，不作为正式项目状态来源。

## Next Task

部署 / 试用收口：上线后只处理**阻断 MVP 使用的 bug**。

Rules:

- 只修上线阻断问题（登录/录入/保存/刷新丢失/数据串模块/假成功这类）。
- 不做 GSC / GA4 / Google Ads OAuth。
- 不做 UI 美化。
- 不做前端模块化。
- 非阻断问题只记 TODO，不现场修。

## Known TODO（非阻断，后续迭代）

- 自动数据源：GSC / GA4 / Google Ads 同步未接入（周报靠人工录入；状态卡如实显示未配置/同步未实现）。
- 部分静态看板真实化：仪表盘 mini 图/donut、GA4 看板等空状态或骨架，待接真实聚合。
- `loadClosedLoop` 加载失败仍静默 catch{}，待补失败提示（与核心 loader 同类）。
- 前端模块化：`public/index.html` 仍是单文件，后续拆分降低维护成本。
- SEO「近 6 周」列现为可编辑文本（非 sparkline）；询盘写入角色为 editor（非仅销售）。

## New Machine Startup Prompt

新电脑或新会话中，先让 Codex / Claude 读取：

```text
请读取 PROJECT_STATUS.md、AGENTS.md、CLAUDE.md、ROADMAP.md。
按 PROJECT_STATUS.md 的 MVP 上线稳定版继续，不要做过度优化。
当前下一步是 MVP 核心数据录入闭环稳定性审计与修复。
完成后只用 5 行交接：Task / Changed files / Checks / Commit / Blockers。
```
