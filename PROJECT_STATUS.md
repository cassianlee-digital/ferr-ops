# ferr-ops Project Status

## UI Follow-up Notes - 2026-06-29

> 来源：用户提供的 `D:\桌面文件\优化文档-6.29.docx`，以及本轮 Codex 继续优化。后续换电脑继续时，先看本段，再看 `AGENTS.md`。

### 用户最新 UI 意见

- 参考阿里云控制台：纯白背景、浅灰分割线、少量蓝色主色、字号克制、模块边界清楚。
- 页面不要“上宽下窄”，模块、表格、按钮、输入框要对齐、等宽、统一密度。
- 不是所有按钮/预选框都要有明显边框；次级操作可以更像文字按钮，避免满屏小框。
- AI 建议里的“沉淀 / 采纳 / 测试”等操作不要竖着挤，占空间且显乱，应横向排列。
- 删除/确认删除按钮不要换行掉到下一行。
- 左侧导航需要调整信息架构：
  - “运营闭环”改为“行动中心”。
  - “任务看板”改为“日计划”。
  - “月度计划”改为“月计划”。
  - “复盘周报”改为“周报”。
  - GA4 后续建议归入“数据看板”页，以 tab 形式放在 SEO / SEM 旁边。
  - 后续考虑把“日计划 / 月计划 / 周报 / 月报”等归成“计划/总结”，把“测试登记 / 整改清单”归成“测试/整改”。

### 本轮已完成

- `public/index.html`
  - 左侧导航已做第一批低风险调整：
    - “任务 / 数据源”改为“数据中心”。
    - “运营闭环”改为“行动中心”。
    - “任务看板”移入“行动中心”，并改名“日计划”。
    - “月度计划”改名“月计划”。
    - “复盘周报”改名“周报”。
  - 计划/总结已做低风险组合：
    - 左侧只保留一个“计划 / 总结”入口。
    - 进入后以 TAB 展示日计划、月计划、周报、月报。
    - 底层仍复用原有月计划、任务看板、复盘周报逻辑，未重写保存/加载/周报/SOP 代码。
- `public/styles.css`
  - 追加全局 UI 收口层：
    - 页面整体背景改为纯白。
    - 顶部栏、品牌区、侧边栏对齐和高度统一。
    - 卡片、表格、弹窗、AI 模块、标签统一为 2px 圆角、浅灰细线、弱阴影/无阴影。
    - 次级按钮、表格小按钮、AI 操作按钮弱化边框，靠文字色和 hover 表达层级。
    - AI 操作区、任务元信息、整改操作列尽量横向排列，减少按钮换行。
    - 删除确认态保持红色但不掉到下一行。
    - 补了移动端基础适配：窄屏下侧栏变为顶部区域，卡片单列，表格横向滚动。
    - 补了深色模式对新增全局样式的兜底。

### 暂未做，建议下一阶段

- GA4 真正嵌入“数据看板”SEO / SEM 同级 tab。
  - 这会移动 DOM 结构和路由状态，建议单独做一轮，避免影响现有 GA4 空状态和加载逻辑。
- “计划/总结”继续深化：
  - 当前已完成同页组合，但内容仍复用原 DOM 模块。
  - 后续可再做页内锚点、月总结区域、真正的二级 tab。
- “测试/整改”新页面：
  - 将测试登记和整改清单做成同一闭环页面，体现测试假设 -> 结果 -> 整改动作。
  - 需要保留原有保存、归档、沉淀逻辑，不要重写业务代码。
- 继续检查真实页面截图：
  - 看是否还有模块上下宽度不齐、按钮换行、表格列挤压、空状态太丑的问题。

## UI Upgrade Handoff - 2026-06-26

> 给下一台电脑 / 下一次 Codex 接续用。继续前请先读本段，再读 `AGENTS.md`。

### 当前 UI 方向

- 风格参考：阿里云控制台，不是营销页、炫酷大屏或花哨 SaaS。
- 核心气质：克制、清晰、可信、专业，像企业后台，不像临时拼出来的小工具。
- 视觉原则：
  - 白底为主，页面背景用很浅的灰。
  - 分割线用浅灰细线，不用大面积阴影和渐变。
  - 主色只保留少量蓝色，用于当前状态、主按钮、焦点输入框。
  - 标题少量加粗，正文保持中等字重，不要到处粗体。
  - 表格、输入框、按钮、标签统一高度、边框、圆角和间距。
  - 页面优先像控制台工作台：信息密度高但不乱，模块边界清楚。
- 禁止方向：
  - 不要做深色科技大屏。
  - 不要大面积彩色渐变、胶囊按钮、浮夸卡片、过重阴影。
  - 不要为了 UI 改接口、改数据保存、改 Google 同步逻辑。
  - 不要加入假数据或 demo 数据冒充真实结果。

### 本轮已经优化到哪里

主要文件：

- `public/styles.css`

已经追加了几组覆盖式样式，目的是在不大拆 `public/index.html` 的前提下，先把观感拉到统一控制台风格：

- 全局基线：
  - 字体、页面背景、主色、边框、按钮、输入框、表格、标签、弹窗、侧边导航做了统一。
  - 目标是把原来“七拼八凑”的视觉先收成一个系统。
- 左侧导航：
  - 已从偏暗、厚重的风格往更干净的后台导航靠拢。
  - 顶部 logo / favicon 已按用户给的红色 logo 做过替换相关改动，注意不要丢。
- 询盘评级里的全球询盘分布：
  - 旋转球已改为平面世界地图方向。
  - 用户最终更认可“蓝色世界地图 + 白色国家边界”的版本。
  - 地图需要继续检查高清显示、线条和标签清晰度，不要再用压扁或脏灰版本。
- 任务看板：
  - 已按阿里云风格收过一轮。
  - 三列任务区、SOP 区、任务卡片、状态标签、空状态、按钮层级已经更规整。
- 月度计划：
  - 已按“左右两个工作区 + 表格主体 + AI 建议区”的方式收过一轮。
  - 表格头、行高、空状态、新增按钮、AI 建议区已降噪。
- 测试登记：
  - 已跟月度计划一起收过一轮。
  - 重点是左右栏一致、表格干净、AI 建议不再像彩色贴片。

### 仍然需要继续优化的页面

下一步优先级建议：

1. 整改清单
   - 当前最乱：表格太挤，截止日期 / 结果 / 操作区域显得硬塞。
   - 建议方向：做成阿里云式数据表格，顶部筛选区、主操作按钮、表格列宽、状态标签统一。
   - 注意：只改样式，不改整改保存、归档、沉淀逻辑。

2. 设置页
   - 当前左侧设置菜单和右侧内容比例还不够像正式后台。
   - 建议方向：左侧为二级菜单，右侧为分组表单，输入项、状态项、说明文字统一。
   - API 接入页要继续诚实显示“未配置 / 待授权 / 未同步 / 失败原因”，不能假装已接入。

3. 数据看板 / KPI 考核
   - 需要统一卡片、表格、仪表盘和空状态。
   - 注意 `AGENTS.md` 已说明部分图表仍存在无数据示例问题，后续要避免假趋势。

4. 复盘 / SOP / 内容资产等剩余页面
   - 按同一套阿里云控制台规则继续收。
   - 每次只做一个页面或一个小区域。

### 右侧工具栏 / Hermes Agent 待办

用户想法：

- 参考阿里云右侧悬浮工具栏。
- 在后台右侧加一列轻量按钮，不要太多。
- 目前想放：
  - 设置按钮
  - Hermes AGENT 智能体按钮
- 点击 Hermes AGENT 后弹出一个面板 / 对话框。
- Hermes AGENT 计划由另一台服务器承载，本后台只做入口和嵌入 / 对接。
- 用户希望它不是普通 AI 分析，而是更像长期管家，能越来越懂业务。

建议实现顺序：

1. 先做纯前端入口壳：
   - 右侧固定窄工具栏。
   - 两个按钮：设置、Hermes AGENT。
   - 点击 Hermes AGENT 打开右下或右侧抽屉面板。
   - 面板先显示“智能体服务未配置 / 等待接入地址”，不要假装能用。

2. 再做配置项：
   - 在设置页增加 Hermes AGENT 服务地址配置。
   - 地址从后端读取，前端不要写死生产地址。

3. 最后做真实对接：
   - 后端代理请求 Hermes 服务器。
   - 加权限校验、失败提示、超时提示。
   - 不在前端暴露任何密钥。

注意：

- 右侧工具栏属于 UI / 交互增强，不应影响现有业务页面。
- 初版不要接真实 agent，先把入口和空状态做好。
- 不要用 iframe 直接硬嵌不可信页面，除非后续确认安全策略。

### 当前本地改动提醒

当前工作区有较多未提交改动，其中不少不是本轮 UI 交接文档产生的。继续前请先看 `git status`，不要随手回退用户或其他助手做过的改动。

已知主要相关文件：

- `public/styles.css`：本轮 UI 覆盖样式主要在这里。
- `public/index.html`：页面结构仍主要集中在这里，尽量不要大拆。
- `public/inquiry-globe.js`：全球询盘分布地图相关。
- `public/ferr-logo.png`：用户给的 logo / 图标资源。
- `public/world.geo.json`：世界地图底图资源。
- `public/closed-loop.js`：闭环页面逻辑，继续 UI 时尽量不要改业务逻辑。

### 继续操作时给 Codex 的推荐提示词

```text
请先读取 PROJECT_STATUS.md 里的 “UI Upgrade Handoff - 2026-06-26” 段，再读取 AGENTS.md。
继续按阿里云控制台风格优化 ferr-ops 后台 UI。
本次只做一个页面：先做“整改清单”。
只允许改样式或必要的轻量结构，不要改接口、数据保存、Google 同步、AI 逻辑。
修改前先说涉及文件，修改后给我：修改摘要 + 测试结果 + TODO。
```

## Current Mode

MVP 上线稳定版。

目标是尽快上线一个可用、稳定、可信的 SEO / SEM 运营后台，让团队先开始录入数据、查看数据、分析问题、生成整改建议。

不要陷入过度优化。当前阶段不追求完整自动化和精细化，只守住核心数据闭环不能出错。

## Working Rules

- 只做对上线有直接价值的事。
- 优先保证核心数据闭环：能新增、能显示、刷新后仍在、数据进入正确模块、失败有提示、不出现假成功。
- 重点避免低级错误：SEO 数据进 SEM、SEM 数据进 SEO、保存后丢失、刷新后消失、API 失败却显示成功。
- AI 建议必须基于真实数据库上下文，能说明问题、证据、建议动作、负责人或验证指标。
- 非阻断问题只记录 TODO，不现场修。
- 不做 GSC / GA4 / Google Ads OAuth，除非用户明确切到自动数据源阶段。
- 不做前端模块化、UI 美化、无关重构。
- 不为“更优雅”改已经能稳定工作的代码。
- Claude / Codex 交接只用 5 行：Task / Changed files / Checks / Commit / Blockers。

## Completed

- Phase 1e-a: `/api/inquiries` 支持 `start_date` / `end_date`，前端时间范围影响询盘。
- Phase 1e-b: `/api/seo-weeks`、`/api/sem-weeks` 支持日期区间；前端仅 SEO 折线图消费区间数据。
- Phase 1e-c: 前端增加时间范围作用边界提示，明确 KPI、SEM 看板、GA4、整改等暂不受时间范围影响。
- 核心数据 loader 失败提示：API 加载失败不再静默残留旧数据，给 toast + 清表。
- 关键词库修复：删除残留静态示例行，列对齐、单元格可真实录入入库。
- 闭环审计通过：核心数据录入 6 模块 + AI 建议采纳到整改/任务，均「新增→刷新仍在、失败有提示、无假成功」。
- 已生成 `DEPLOY_CHECKLIST.md`（启动/.env/首次部署/备份/手测/已知 TODO）。

## MVP Validation

- 已按 `DEPLOY_CHECKLIST.md` 完成上线前手测，结果：**登录、询盘、SEO 周报、SEM 周报、关键词、否词、广告创意、AI 建议采纳到整改/任务 —— 均无阻断问题**。
- 结论：**MVP 可进入部署 / 试用阶段。**

Latest pushed commit:

- `a55e623 docs: add deploy checklist`

## Known Local Difference

- `FERR运营后台_需求文档.md` 是用户主动删除的旧文件，不要恢复。
- 是否提交这个删除由用户单独决定，不要混入功能任务。
- `.claude/` 是本机协作文件，不作为正式项目状态来源。

## Next Task

部署 / 试用收口：上线后只处理**阻断 MVP 使用的 bug**。

Rules:

- 只修上线阻断问题（登录/录入/保存/刷新丢失/数据串模块/假成功这类）。
- 不做 GSC / GA4 / Google Ads OAuth。
- 不做 UI 美化。
- 不做前端模块化。
- 非阻断问题只记 TODO，不现场修。

## Known TODO（非阻断，后续迭代）

- 自动数据源：GSC / GA4 / Google Ads 同步未接入（周报靠人工录入；状态卡如实显示未配置/同步未实现）。
- 部分静态看板真实化：仪表盘 mini 图/donut、GA4 看板等空状态或骨架，待接真实聚合。
- `loadClosedLoop` 加载失败仍静默 catch{}，待补失败提示（与核心 loader 同类）。
- 前端模块化：`public/index.html` 仍是单文件，后续拆分降低维护成本。
- SEO「近 6 周」列现为可编辑文本（非 sparkline）；询盘写入角色为 editor（非仅销售）。

## UI Update - 2026-06-29 Plan / Summary

- 「计划 / 总结」左侧入口已改为一个统一页面内的四个 TAB：日计划、月计划、周报、月报。
- 日计划复用原「任务看板」，月计划复用原「月度计划」，周报复用原「复盘周报」。
- 月报当前先复用周报的展示结构，并把页面文案从「本周 / 下周 / 每周」替换为「本月 / 下月 / 每月」；为避免误写周报数据，月报里的编辑、新增、采纳、测试按钮已做只读处理。
- 本轮只改前端结构和样式，不改数据接口、不改保存逻辑、不改 Google 同步逻辑。

## UI Update - 2026-06-29 Test / Fix

- 「测试登记」和「整改清单」已合并为左侧一个「测试 / 整改」入口。
- 入口内新增两个 TAB：测试登记、整改清单。
- 测试登记继续复用原测试模块，整改清单继续复用原整改模块；未重写保存、归档、AI 生成、采纳沉淀等业务逻辑。
- 本轮只做页面组织和样式收口，让测试假设 -> 整改动作更像一个闭环工作台。

## UI Update - 2026-06-29 Finish Pass

- 已追加全局 UI 收尾层，集中处理按钮、标签、表格、输入框、TAB、卡片边框与字号密度。
- 移动端补了基础适配：主内容留白收窄、顶部 KPI 横向滚动、TAB/筛选/按钮横向滚动、表格在卡片内横向滚动。
- 深色模式补了兜底：卡片、表格、按钮、输入框、提示文字、计划/总结与测试/整改外壳都有对应暗色边框和底色。
- 仍然只改样式和交接记录，不改接口、不改保存逻辑、不改 Google 同步、不增加假数据。

## UI Update - 2026-06-29 Settings

- 设置页继续按阿里云控制台风格收口：左侧设置目录、右侧配置内容、卡片标题区、数据源/API 状态行、SOP 表格按钮做了统一。
- 设置页说明文案改为强调账号、指标、数据源、API 接入、SOP 集中配置，并明确密钥不进入前端。
- 补了设置页移动端和深色模式细节；本轮不改任何接口、密钥存储、Google 授权或同步逻辑。

## UI Update - 2026-06-29 KPI / Data Dashboard

- KPI 考核页继续收口：三张总分卡、明细列表、询盘分布卡的高度、行距、分割线和数字权重统一。
- 数据看板页继续收口：SEO/SEM 主 TAB、二级 TAB、AI 周报块、指标卡、层级表格按阿里云控制台风格弱化边框和色彩。
- 表格补了横向滚动与移动端折行兜底；本轮只改样式，不改图表数据、不改诊断逻辑、不增加示例数据。

## UI Update - 2026-06-29 Asset Library

- 关键词库、内容资产、归档页继续按阿里云控制台列表页风格收口。
- 统一了资产库页面的 TAB、工具条、表格头部、标签、分页、空状态和操作按钮密度。
- 表格统一走横向滚动兜底，移动端不强行挤压列；本轮只改样式，不改关键词、内容资产、归档恢复/删除逻辑。

## UI Update - 2026-06-29 Inquiry

- 询盘评级页继续收口：页面头部、时间筛选、提示条、询盘表格、标签、月份分隔行统一为控制台风格。
- 全球询盘分布地图卡片做了边界、标题栏、高度比例、底部国家统计、悬浮提示的视觉统一。
- 本轮不改询盘保存/删除/归档逻辑，不改地图国家聚合和飞线 hover 逻辑。

## UI Update - 2026-06-29 Overview / Rail

- 总览页继续收口：顶部 KPI 胶囊、运营总览卡片、KPI 总分区、SEO/SEM 概览、Top 关键词表、总览 AI 诊断块统一为控制台风格。
- 右侧工具栏和 Hermes 智能体面板继续弱化圆角、阴影和杂色，保持“入口壳”状态，不接真实 Agent 服务。
- 本轮只改样式，不改总览数据加载、AI 诊断、Hermes 面板打开关闭逻辑。

## UI Update - 2026-06-29 Market / Deposit / GA4

- 市场分析、沉淀表、GA4 流量页做了最后一轮控制台风格收口：卡片横向滚动、表格行高、可编辑单元格提示、按钮尺寸、空状态和滚动条统一。
- GA4 继续保持诚实空状态，只优化展示密度和提示样式；没有添加示例流量，也没有改 GA4 授权、同步或接口逻辑。
- 补齐了这几个页面的移动端折叠和深色模式兜底，避免小屏下标题、按钮、表格列互相挤压。

## UI Update - 2026-06-29 Modal / Form Polish

- 弹窗、表单输入框、下拉框、文本域、关闭按钮、底部按钮和 Toast 提示统一为阿里云控制台式的 2px 圆角、浅灰边框、克制阴影。
- 补齐弹窗移动端表现：小屏下弹窗满宽、表单双列改单列、底部按钮自动换行，避免输入区域被挤压。
- 本轮只改样式，不改新增/保存/修改密码/AI 弹窗/询盘录入等任何业务逻辑。

## UI Update - 2026-06-29 Controls / Inline Details

- 时间筛选、粒度切换、分类 TAB、分页、小操作按钮、AI 操作按钮、彩色标签、表格内日期输入和可编辑单元格做了统一收口。
- 小控件统一为 30px 左右高度、2px 圆角、浅灰边框、蓝色 hover/active，减少页面里按钮和标签各长各的感觉。
- 移动端下筛选条、工具条、分类 TAB 改为横向滚动，避免窄屏强行挤压换行。
- 本轮只改样式，不改时间筛选逻辑、标签切换逻辑、分页逻辑、AI 采纳/沉淀/测试逻辑。

## UI Update - 2026-06-30 Page Rhythm

- 继续按阿里云控制台风格统一页面节奏：页面左右留白、顶部标题区、标题/副标题字号、卡片标题栏、模块间距和表格行高做了全局兜底。
- 重点处理“同一个后台里每个页面上下间距不一样”的问题，让页面从标题到工具条、卡片、表格的节奏更一致。
- 移动端下标题区改为纵向排布，顶部按钮满宽居中，避免标题和按钮挤在同一行。
- 本轮只改样式，不改页面结构、不改接口、不改保存/同步/AI 逻辑。

## UI Update - 2026-06-30 Empty / Notice States

- 继续统一空状态、提示条、状态徽标和风险提示：无数据区域改为浅灰虚线容器，普通说明为浅蓝提示，真实风险/失败仍保留红色状态。
- 统一了灰、蓝、绿、黄、红徽标的边框、底色和字重，让状态表达更克制，不再像临时彩色贴纸。
- 补齐这些状态在深色模式和移动端下的兜底样式。
- 本轮只改样式，不改空状态显隐逻辑、不改 Google/GA4/GSC/Ads 接入状态判断、不添加任何假数据。

## UI Fix - 2026-06-30 Logo / AI Actions

- 修复左侧品牌区 logo / favicon 的缓存与显示兜底：继续使用 `public/ferr-logo.png`，追加版本号避免浏览器继续读旧图标，并强制按原图显示，不再被背景、裁切、滤镜影响。
- 修复 AI 建议下方「沉淀 / 采纳 / 测试」按钮排版：桌面端固定在建议文字右侧横向排列，小屏幕下才换到下一行，避免按钮挤进左侧窄栏。
- 本轮只改图标引用和样式兜底，不改 AI 采纳、沉淀、测试逻辑，不改后端接口和数据保存逻辑。

## UI / Logic Update - 2026-06-30 Planning Review

- 「计划 / 总结」页收掉了外层大框和重复模块边框，保留轻量分割线和内部留白，减少文字贴边、框套框的问题。
- 周报逻辑改为每月最多 4 个周序：第 5 周会自动归到下个月第 1 周；旧数据里的 `2026-6-5` 这类 key 在界面显示时也会兼容成 `2026-7-1`，避免再出现「6月第5周」。
- 周报当前月按周展示：本周按时间自动展开；同月历史周折叠；历史月份按「YYYY年M月周报」折叠归组。
- 周报展开时间规则：每周一 08:00 后自动展开本周；周五 22:00 后自动折叠。
- 月报改为独立渲染和独立保存，不再复制周报只读结构；当前月展开，历史月折叠，字段文案为「本月 / 下月」。
- 本轮复用原 `weekly_reports` 接口和保存逻辑，没有改数据库结构、权限、Google 同步或 AI 逻辑。

## UI Update - 2026-06-30 Action / Alignment Pass

- 测试 / 整改入口继续按阿里云控制台风格收口：去掉外层大框和重复容器边框，保留轻量 TAB、标题分割线、表格边线和 AI 建议区边界，减少“框套框”的廉价感。
- 关键词库、归档等列表页补了子模块左右对齐：标题区、TAB、工具条、表格模块尽量共用同一内容宽度，不再出现下面模块比上面标题缩进去一截的视觉问题。
- 运营总览里的 SEO / SEM 总览卡片补了内部留白：卡片标题栏和三组指标之间增加间距，指标数字、标签、涨跌幅不再贴着标题左上角。
- 本轮只改样式收口，不改测试登记、整改清单、关键词库、总览数据加载、AI 采纳、归档或任何 Google 同步逻辑。

## UI Update - 2026-06-30 GA4 Data Tab

- GA4 流量从左侧独立导航合并到「数据看板」内，和「SEO 看板 / SEM 看板」同级作为第三个 TAB 展示。
- 复用原 GA4 看板 DOM 与 `loadGa4()` 数据加载逻辑，只调整入口挂载和嵌入样式，不改 GA4 接口、不改同步逻辑、不添加示例数据。
- 兼容旧本地路由：如果浏览器曾记住 `GA4 流量` 独立入口，刷新后会自动切到「数据看板 → GA4 流量」。

## UI Fix - 2026-06-30 Archive Table Columns

- 归档页 SEM / SEO / 公司任务 / 询盘表格改为归档专属列宽规则：归档时间、来源、部门、操作固定宽度，内容列自适应。
- 修复归档时间列过窄导致日期换行的问题；内容列改为单行预览省略，鼠标悬停可看完整文本。
- 本轮只改归档表格 DOM class 和样式，不改归档加载、恢复、彻底删除、询盘恢复或后端接口逻辑。

## New Machine Startup Prompt

新电脑或新会话中，先让 Codex / Claude 读取：

```text
请读取 PROJECT_STATUS.md、AGENTS.md、CLAUDE.md、ROADMAP.md。
按 PROJECT_STATUS.md 的 MVP 上线稳定版继续，不要做过度优化。
当前下一步是 MVP 核心数据录入闭环稳定性审计与修复。
完成后只用 5 行交接：Task / Changed files / Checks / Commit / Blockers。
```
